{
  "name": "darkfactory-project",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "test:factory": "vitest run --reporter=default --reporter=json --outputFile=test-results/vitest.json"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
